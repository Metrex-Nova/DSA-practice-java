class Solution {
    public int largestElement(int[] nums) {
        int a = Integer.MIN_VALUE;
        for(int i = 0; i<nums.length; i++){
            if(nums[i] > a) {
                a = nums[i];
            }
        }
        return a;
    }
}
