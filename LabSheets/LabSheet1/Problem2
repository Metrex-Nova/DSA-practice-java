Q2: Matrix Multiplication
Description:
Write a program to multiply two matrices.
1. Read two matrices A and B
2. Check if multiplication is possible (Condition: C1 == R2). If not, print "Not Possible".
3. Implement a function multiplyMatrices() that computes the product.
4. Print the resulting matrix.
Input Format:
● Dimensions of A (R1, C1), followed by elements of A.
● Dimensions of B (R2, C2), followed by elements of B.
Sample Test Case :
Input:
2 2
1 2
3 4
2 2
1 0
0 1
Output:
1 2
3 4


#include <stdio.h>

void multiplyMatrices(int r1, int c1, int A[r1][c1], int r2, int c2, int B[r2][c2], int result[r1][c2]){
    // vocalization
    // for each row of the first matrix and for each column of the second matrix you perform traversal in such a way that any arbitrary element of the reultant matrix is composed of summations of multiplication of elements respectively form first and second matrix, keeping the middle address same.
    for(int i =0; i<r1; i++){
        for(int j = 0; j<c2; j++){
            result[i][j] = 0;
            for(int k = 0; k<c1; k++){
                result[i][j] = result[i][j] + A[i][k] * B[k][j];
            }
        }
    }
}

int main(){
    printf("Hello World!");
    int r1, c1;
    scanf("%d %d", &r1, &c1);
    int A[r1][c1];
    for(int i = 0; i<r1; i++){
        for(int j = 0 ; j<c1; j++){
            scanf("%d", &A[i][j]);
        }
    }
    int r2, c2;
    scanf("%d %d", &r2, &c2);
    int B[r2][c2];
    for(int k = 0; k<r2; k++){
        for(int l = 0; l<c2; l++){
            scanf("%d", &B[k][l]);
        }
    }
    int result[r1][c2];
    multiplyMatrices(r1, c1, A, r2, c2, B, result);
    
    for(int i = 0; i<r1; i++){
        for(int j = 0; j<c2; j++){
            printf("%d ", result[i][j]);
        }
        printf("\n");
    }
    
    return 0;
}
