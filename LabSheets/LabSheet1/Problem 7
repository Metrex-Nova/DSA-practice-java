Q7: String Compression
Description:
Write a program to compress a string by replacing consecutive identical characters with the
character followed by its count.
● Example: aaabbcbecomes a3b2c1.
● If the compressed string is not smaller than the original (eg, abc-> a1b1c1), print the
original string.
Input Format:
● A single string (no spaces).
Sample Test Case 1:
Input:
aaabbc
Output:
a3b2c1
Sample Test Case 2:
Input:
aabb
Output:
a2b2


#include <stdio.h>
#include <string.h>
int main(){
    printf("Hello World");
    char str[100];
    char compressed[200];
    
    printf("\nPlease enter the string here: ");
    scanf("%s", str);
    int n = strlen(str);
    int i = 0;
    int a = 0;
    while(i<n){
        char current = str[i];
        int count = 1;
        while(i+1 < n && str[i] == str[i+1]){
            i++;
            count++;
        }
        printf("%c%d", str[i], count);
        a = a + 2;
        i++;
    }
    if(n<=a){
        printf("\n\nThe compressed string is not smaller than the original: ");
        printf("\nOrignal string: %s", str);
    }
    return 0;
}
